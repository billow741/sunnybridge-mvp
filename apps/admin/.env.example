# SunnyBridge Admin — Environment Variables
# Copy to .env for local development

# API base URL
# Local dev: uses Vite proxy → /api/v1
# Production: full URL → https://api.sunnybridge.qzz.io/api/v1
VITE_API_BASE_URL=/api/v1
